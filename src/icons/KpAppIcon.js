import React from "react"
import { AppIcon } from "@kit-ui/icons"
import theme from "../app/kp/theme"
const KpAppIcon = (props) => <AppIcon {...props} variant="outlined" color={theme.palette.primary.main} text="Kp" />
export default KpAppIcon