import schema from "./ImageEditor.schema"
import uiSchema from "./ImageEditor.uiSchema"
import formData from "./ImageEditor.formData"

export default {
    schema: schema,
    uiSchema: uiSchema,
    formData: formData
}