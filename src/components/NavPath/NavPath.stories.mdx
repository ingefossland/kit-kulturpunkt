import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';

import Path from "./Path"
import PathItem from "./PathItem"
import NavPath from "./NavPath"

<Meta title="Navigation/NavPath" component={NavPath}/>

# NavPath

Parents, title, filters and search.

## Properties

<Props of={NavPath} />

NavPath takes an array of parents + title:

<Preview>
    <Story name="Parents + title">
        <NavPath 
            parents={[
                {
                    "label": "Grandparent",
                    "url": "/grand"
                },
                {
                    "label": "Parent",
                    "url": "/parent"
                }
            ]}
            title="Current" 
            onSelect={action('onSelect')} />
    </Story>
</Preview>

NavPath takes an array of parents + title:

<Preview>
    <Story name="Parents + filters">
        <NavPath 
            parents={[
                {
                    "label": "Grandparent",
                    "url": "/grand"
                },
                {
                    "label": "Parent",
                    "url": "/parent"
                }
            ]}
            filters={[
                {
                    "label": "I dag",
                    "value": "today",
                    "url": "/grand/today"
                },
                {
                    "label": "I går",
                    "value": "yesterday",
                    "url": "/grand/yesterday"
                },
                {
                    "label": "Siste uke",
                    "value": "week",
                    "url": "/grand/week"
                }
            ]} 
            onSelect={action('onSelect')} />
    </Story>
</Preview>

## Composition

<Preview>
    <Story name="Composition">
        <Path>
            <PathItem>Grandparent</PathItem>
            <PathItem>Parent</PathItem>
        </Path>
    </Story>
</Preview>