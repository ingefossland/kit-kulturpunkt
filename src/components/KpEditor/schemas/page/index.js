import schema from "./schema";
import uiSchema from "./uiSchema";

export default {
    id: 1,
    schemaType: 'documents',
    name: 'page',
    schema: schema,
    uiSchema: uiSchema,
    preview: {
//        template: PreviewTemplate
    }
}