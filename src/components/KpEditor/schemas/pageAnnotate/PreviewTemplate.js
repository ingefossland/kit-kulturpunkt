import React from 'react';
import Annotate from "../../../KpPreview/Annotate"

const PageAnnotatePreview = ({formData, formContext}) => {

    return <Annotate formData={formData} formContext={formContext} />

}

export default PageAnnotatePreview