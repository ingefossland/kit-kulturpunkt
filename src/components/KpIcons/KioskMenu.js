import React from "react"
import { AppIcon } from "@kit-ui/icons"
import MenuIcon from "./MenuIcon"
const KioskMenuIcon = (props) => <MenuIcon {...props} variant="filled" text="Ki" />
export default KioskMenuIcon