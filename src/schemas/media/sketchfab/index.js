import schema from "./schema";
import uiSchema from "./uiSchema";

export default {
    id: 1,
    schemaType: 'media',
    name: 'application/sketchfab',
    schema: schema,
    uiSchema: uiSchema,
}