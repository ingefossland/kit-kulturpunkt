"use strict";

exports.__esModule = true;
exports.default = exports.getObjectToolbar = void 0;

var getObjectToolbar = function getObjectToolbar(props) {
  var toolbar = [];

  if (props.hasToolbar) {
    if (props.hasMoveUp || props.hasMoveDown) {
      // move up
      toolbar.push({
        icon: "arrow_upward",
        disabled: props.disabled || props.readonly || !props.hasMoveUp,
        onClick: props.onReorderClick(props.index, props.index - 1)
      }); // move down

      toolbar.push({
        icon: "arrow_downward",
        disabled: props.disabled || props.readonly || !props.hasMoveDown,
        onClick: props.onReorderClick(props.index, props.index + 1)
      });
    } // remeove, delete, etc


    if (props.hasRemove) {
      toolbar.push({
        icon: "remove_circle",
        disabled: props.disabled || props.readonly,
        onClick: props.onDropIndexClick(props.index)
      });
    }
  }

  return toolbar;
};

exports.getObjectToolbar = getObjectToolbar;
var _default = getObjectToolbar;
exports.default = _default;