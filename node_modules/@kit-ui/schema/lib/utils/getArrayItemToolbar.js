"use strict";

exports.__esModule = true;
exports.default = exports.getArrayItemToolbar = void 0;

var getArrayItemToolbar = function getArrayItemToolbar(props, toolbar) {
  if (toolbar === void 0) {
    toolbar = [];
  }

  if (props.hasToolbar) {
    if (props.hasMoveUp || props.hasMoveDown) {
      // move up
      toolbar.push({
        icon: "arrow_upward",
        disabled: props.disabled || props.readonly || !props.hasMoveUp,
        onClick: props.onReorderClick(props.index, props.index - 1)
      }); // move down

      toolbar.push({
        icon: "arrow_downward",
        disabled: props.disabled || props.readonly || !props.hasMoveDown,
        onClick: props.onReorderClick(props.index, props.index + 1)
      });
    }
    /*
     if (props.hasRemove) {
         toolbar.push({
            icon: "remove_circle",
            disabled: props.disabled || props.readonly,
            onClick: props.onDropIndexClick(props.index)
        })
        
    }
    */

  }

  return toolbar;
};

exports.getArrayItemToolbar = getArrayItemToolbar;
var _default = getArrayItemToolbar;
exports.default = _default;