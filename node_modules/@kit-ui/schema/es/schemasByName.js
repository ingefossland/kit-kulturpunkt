import mediaSchemas from "./Media/schemas";
var schemas = [].concat(mediaSchemas);
var schemasByName = {};
schemas.map(function (schema) {
  var schemaType = schema.schemaType,
      name = schema.name;

  if (schemaType && name) {
    schemasByName[schemaType + "/" + name] = schema;
  }
});
export default schemasByName;