export var getFormDataFromRRule = function getFormDataFromRRule(rrule) {
  var rules = rrule && rrule.length && rrule.split(";");
  var formData = {};
  rules && rules.map(function (rule) {
    var rrule = rule.split("=");
    var name = rrule[0];
    var value = rrule[1];

    if (name && value) {
      if (name === "BYDAY" || name === "BYMONTH" || name === "BYMONTHDAY" || name === "BYSETPOS") {
        formData[name] = value.split(",");
      } else {
        formData[name] = value;
      }
    }
  });
  return formData;
};