import { RRule, RRuleSet, rrulestr } from 'rrule';
export var getRRuleText = function getRRuleText(rrule) {
  var rule;

  if (rrule) {
    rule = RRule.fromString("RRULE:" + rrule);
  }

  return rule && rule.toText();
};
export default getRRuleText;