import React from 'react';
import FixedArrayFieldTemplate from "./FixedArrayFieldTemplate";
import NormalArrayFieldTemplate from "./NormalArrayFieldTemplate";
import SortableArrayFieldTemplate from "./SortableArrayFieldTemplate";
import { utils } from '@rjsf/core';
var isMultiSelect = utils.isMultiSelect,
    getUiOptions = utils.getUiOptions,
    getDefaultRegistry = utils.getDefaultRegistry;

var ArrayFieldTemplate = function ArrayFieldTemplate(props) {
  var schema = props.schema,
      _props$registry = props.registry,
      registry = _props$registry === void 0 ? getDefaultRegistry() : _props$registry;
  var uiOptions = getUiOptions(props.uiSchema);

  if (isMultiSelect(schema, registry.rootSchema)) {
    return /*#__PURE__*/React.createElement(FixedArrayFieldTemplate, props);
  } else if (uiOptions.sortable || uiOptions.draggable) {
    return /*#__PURE__*/React.createElement(SortableArrayFieldTemplate, props);
  } else {
    return /*#__PURE__*/React.createElement(NormalArrayFieldTemplate, props);
  }
};

export default ArrayFieldTemplate;