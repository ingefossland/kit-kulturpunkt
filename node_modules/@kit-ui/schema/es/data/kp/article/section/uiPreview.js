export default {
  "select": {
    //        "icon": "icon",
    "title": "title"
  },
  prepare: function prepare(_ref) {
    var formData = _ref.formData;
    var localeId = "locale:no";
    var title = formData.title && formData.title[localeId] || "Untitled";
    var sectionType = formData.sectionType;
    var icons = {
      "text": "subject",
      "media": "image",
      "objects": "museum"
    };
    return {
      //            icon: icons && icons[sectionType],
      title: title
    };
  }
};