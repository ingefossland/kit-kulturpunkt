export default {
  "ui:layout": "section",
  "ui:title": "Presentasjon",
  "ui:preview": {
    "select": {
      "description": "description"
    },
    prepare: function prepare(_ref) {
      var formData = _ref.formData,
          formContext = _ref.formContext;
      var title = formData.title;
      var localeId = "locale:" + formContext.currentLocale;
      var description = title && title[localeId];
      return {
        description: description
      };
    }
  },
  "ui:fieldset": ["title"],
  "ui:options": {
    "expanded": true,
    "collapsible": true,
    "padding": 2,
    "spacing": 2,
    "grid": true
  },
  "title": {
    "ui:title": "Tittel"
  }
};