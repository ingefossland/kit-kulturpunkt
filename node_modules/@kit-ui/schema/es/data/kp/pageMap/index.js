import schema from "./schema";
import uiSchema from "./uiSchema"; //import PreviewTemplate from "../../preview/PageMap"

export default {
  id: 1,
  schemaType: 'kp',
  name: 'page/map',
  schema: schema,
  uiSchema: uiSchema,
  preview: {//        template: PreviewTemplate
  }
};