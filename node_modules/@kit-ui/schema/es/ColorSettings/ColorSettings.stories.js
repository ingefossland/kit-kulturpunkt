import React from 'react';
import Schema from "../SchemaEditor/";
export default {
  title: 'Settings/ColorSettings',
  component: Schema,
  args: {
    schema: {
      type: "object",
      properties: {
        color: {
          "type": "string",
          "format": "color"
        },
        colorEnum: {
          "type": "string",
          "format": "color",
          "enum": ["#ff0", "#f0f", "#00f", "red", "blue", "green", "orange", "pink"]
        }
      }
    },
    uiSchema: {
      "ui:layout": "module",
      "ui:options": {
        "editable": true
      },
      "ui:settings": ["color", "colorEnum"],
      "color": {}
    }
  },
  argTypes: {}
};

var Template = function Template(args) {
  return /*#__PURE__*/React.createElement(Schema, args);
};

export var Default = Template.bind({});
Default.args = {};