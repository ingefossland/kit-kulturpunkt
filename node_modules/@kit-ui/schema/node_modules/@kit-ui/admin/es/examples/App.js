import React, { useState, useEffect } from 'react';
import { AppLayout } from "../";
import { getRandomUser, getRandomOwner, getRandomTheme, getRandomApp } from "../utils";

var App = function App() {
  var user = getRandomUser();
  var owner = getRandomOwner();
  var theme = getRandomTheme();
  var app = getRandomApp();

  var _useState = useState(""),
      q = _useState[0],
      setQ = _useState[1];

  var search = {
    placeholder: "Søk i " + app.title,
    q: q
  };
  return /*#__PURE__*/React.createElement(AppLayout, {
    app: app,
    theme: theme,
    search: search
  }, "App example");
};

export default App;