import React from 'react';
import Calendar from "./Calendar";
import Timetable from "./Timetable";
import Schedule from "./Schedule";
var templates = {
  "calendar": Calendar,
  "timetable": Timetable,
  "schedule": Schedule
};

var CalendarLayout = function CalendarLayout(_ref) {
  var date = _ref.date,
      _ref$layout = _ref.layout,
      layout = _ref$layout === void 0 ? "calendar" : _ref$layout;
  var CalendarTemplate = templates && templates[layout];
  return /*#__PURE__*/React.createElement(CalendarTemplate, {
    date: date
  });
};

export default CalendarLayout;