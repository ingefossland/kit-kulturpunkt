import React from 'react';
import AppsByOwner from './AppsByOwner';
import { getRandomOwners, getRandomOwner } from "..";
var randomOwner = getRandomOwner();
export default {
  title: 'Components/AppSwitcher/AppsByOwner',
  component: AppsByOwner,
  args: {
    owners: getRandomOwners(20)
  },
  argTypes: {}
};

var Template = function Template(args) {
  return /*#__PURE__*/React.createElement(AppsByOwner, args);
};

export var OwnerList = Template.bind({});
OwnerList.args = {
  owners: getRandomOwners(20)
};
export var CurrentOwner = Template.bind({});
CurrentOwner.args = {
  currentOwner: getRandomOwner()
};