import React from 'react';
import AudioControls from './AudioControls';
export default {
  title: 'Media/AudioControls',
  component: AudioControls,
  args: {
    title: "Take on me",
    description: "A-ha",
    duration: 210
  },
  argTypes: {}
};

var Template = function Template(args) {
  return /*#__PURE__*/React.createElement(AudioControls, args);
};

export var Default = Template.bind({});
Default.args = {};
export var Paused = Template.bind({});
Paused.args = {
  paused: true
};