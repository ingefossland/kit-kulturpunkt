import React from 'react';
import { makeStyles } from '@material-ui/core/styles';
import MediaVolumeButton from "./MediaVolumeButton";
import MediaVolumeSlider from "./MediaVolumeSlider";
var useStyles = makeStyles(function (theme) {
  return {
    root: {
      display: "flex",
      alignItems: "center"
    }
  };
});

var AudioControls = function AudioControls(_ref) {
  var className = _ref.className,
      muted = _ref.muted,
      volume = _ref.volume,
      onClick = _ref.onClick,
      onChange = _ref.onChange;
  var classes = useStyles();
  return /*#__PURE__*/React.createElement("div", {
    className: className || classes.root
  }, /*#__PURE__*/React.createElement(MediaVolumeButton, {
    muted: muted,
    onClick: onClick
  }), /*#__PURE__*/React.createElement(MediaVolumeSlider, {
    muted: muted,
    min: 0,
    max: 1,
    value: volume,
    onChange: onChange
  }));
};

export default AudioControls;