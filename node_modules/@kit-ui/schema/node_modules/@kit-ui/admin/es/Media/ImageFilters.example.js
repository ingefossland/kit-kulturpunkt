import React, { useState } from "react";
import ImageFilters from "./ImageFilters";
import EditorLayout from "../Editor/EditorLayout";

var ImageFiltersExample = function ImageFiltersExample(_ref) {
  var imageUrl = _ref.imageUrl;

  var _useState = useState({}),
      imageFilters = _useState[0],
      setImageFilters = _useState[1];

  var handleChange = function handleChange(imageFilters) {
    setImageFilters(imageFilters);
  };

  return /*#__PURE__*/React.createElement(EditorLayout, {
    title: "ImageFilters",
    preview: {
      children: /*#__PURE__*/React.createElement("p", null, JSON.stringify(imageFilters))
    }
  }, /*#__PURE__*/React.createElement(ImageFilters, {
    imageUrl: imageUrl,
    imageFilters: imageFilters,
    onChange: handleChange
  }));
};

export default ImageFiltersExample;