import React from 'react';
import VideoControls from './VideoControls';
export default {
  title: 'Media/VideoControls',
  component: VideoControls,
  args: {
    title: "Take on me",
    description: "A-ha",
    duration: 210
  },
  argTypes: {}
};

var Template = function Template(args) {
  return /*#__PURE__*/React.createElement(VideoControls, args);
};

export var Default = Template.bind({});
Default.args = {};
export var Paused = Template.bind({});
Paused.args = {
  paused: true
};