import React from "react";
import ProfileIconMale from "./ProfileIconMale";
import ProfileIconFemale from "./ProfileIconFemale";
var variants = {
  "male": ProfileIconMale,
  "female": ProfileIconFemale
};

var ProfileIcon = function ProfileIcon(_ref) {
  var _ref$variant = _ref.variant,
      variant = _ref$variant === void 0 ? "female" : _ref$variant,
      fontSize = _ref.fontSize;
  var IconTemplate = variants && variants[variant];

  if (!IconTemplate) {
    return "<p>Failed</p>";
  }

  return /*#__PURE__*/React.createElement(IconTemplate, null);
};

export default ProfileIcon;