import React, { useState, useRef } from 'react';
import NavLocale from "../NavLocale/NavLocale";

var EditorLocale = function EditorLocale(_ref) {
  var className = _ref.className,
      _ref$currentLocale = _ref.currentLocale,
      currentLocale = _ref$currentLocale === void 0 ? undefined : _ref$currentLocale,
      _ref$languages = _ref.languages,
      languages = _ref$languages === void 0 ? [] : _ref$languages,
      onLocale = _ref.onLocale;

  if (!languages.length || languages.length === 1) {
    return null;
  }

  return /*#__PURE__*/React.createElement(NavLocale, {
    options: languages,
    value: currentLocale || languages[0],
    onChange: onLocale
  });
};

export default EditorLocale;