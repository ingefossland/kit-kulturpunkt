import Color from 'color';
export function getContrastColor(color) {
  color = Color(color);

  if (color.isDark()) {
    return "white";
  } else {
    return "black";
  }
}
export default getContrastColor;