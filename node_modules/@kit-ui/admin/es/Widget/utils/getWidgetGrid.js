export var getWidgetGrid = function getWidgetGrid(size) {
  if (!size) {
    return {};
  }

  var sizes = {
    "xs": 2,
    "s": 4,
    "m": 8,
    "l": 12,
    "xl": 16
  };
  var grid = size.split(":");
  var cols = grid[0];
  var rows = grid[1];
  cols = sizes[cols] || parseInt(cols);
  rows = sizes[rows] || parseInt(rows);
  return {
    cols: cols,
    rows: rows
  };
};
export default getWidgetGrid;