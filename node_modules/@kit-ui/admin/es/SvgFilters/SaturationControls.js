import React from "react";
import SliderControl from './SvgFilterSliderControl';

var SaturationControls = function SaturationControls(_ref) {
  var id = _ref.id,
      className = _ref.className,
      value = _ref.value,
      onChange = _ref.onChange;

  var handleChange = function handleChange(event, value) {
    onChange && onChange({
      value: value
    });
  };

  return /*#__PURE__*/React.createElement("div", {
    className: className
  }, /*#__PURE__*/React.createElement(SliderControl, {
    id: id,
    label: "Opacity",
    value: value,
    min: -100,
    max: 100,
    onChange: handleChange
  }));
};

SaturationControls.defaultProps = {
  value: 100
};
export default SaturationControls;